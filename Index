<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Moonveil | All-In-One Discord Bot</title>
  <style>
    :root {
      --bg: #0f1220;
      --card: #171a2e;
      --accent: #7c7cff;
      --text: #e9ebff;
      --muted: #aab0ff;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: linear-gradient(180deg, #0b0e1a, #0f1220);
      color: var(--text);
    }
    header {
      position: sticky; top: 0; z-index: 10;
      background: rgba(15,18,32,.85); backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255,255,255,.06);
    }
    .nav {
      max-width: 1100px; margin: auto; padding: 14px 16px;
      display: flex; align-items: center; justify-content: space-between;
    }
    .brand { font-weight: 800; letter-spacing: .4px; }
    .brand span { color: var(--accent); }
    .nav a {
      color: var(--text); text-decoration: none; margin-left: 14px; opacity: .9;
    }
    .nav a:hover { opacity: 1; color: var(--accent); }section { max-width: 1100px; margin: auto; padding: 64px 16px; }

.hero {
  display: grid; grid-template-columns: 1.2fr .8fr; gap: 24px; align-items: center;
}
.hero h1 { font-size: 44px; margin: 0 0 12px; }
.hero p { color: var(--muted); font-size: 18px; }
.cta {
  display: inline-block; margin-top: 16px; padding: 12px 18px; border-radius: 10px;
  background: linear-gradient(135deg, var(--accent), #5c5cff);
  color: #0b0e1a; font-weight: 700; text-decoration: none;
  box-shadow: 0 10px 30px rgba(124,124,255,.35);
}
.card {
  background: linear-gradient(180deg, #171a2e, #14172a);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 16px; padding: 20px;
}
.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 16px; }
.feature h3 { margin-top: 0; }

.commands li {
  list-style: none; margin: 10px 0; padding: 12px; border-radius: 12px;
  background: rgba(255,255,255,.04); border: 1px solid rgba(255,255,255,.06);
}
.commands code { color: var(--accent); font-weight: 700; }

.status { display: flex; gap: 16px; flex-wrap: wrap; }
.pill {
  padding: 10px 14px; border-radius: 999px; background: rgba(124,124,255,.15);
  border: 1px solid rgba(124,124,255,.35);
}

footer {
  border-top: 1px solid rgba(255,255,255,.06);
  padding: 24px 16px; text-align: center; color: var(--muted);
}

@media (max-width: 860px) {
  .hero { grid-template-columns: 1fr; }
  .hero h1 { font-size: 36px; }
}

  </style>
</head>
<body>
  <header>
    <div class="nav">
      <div class="brand">Moon<span>veil</span></div>
      <nav>
        <a href="#home">Home</a>
        <a href="#features">Features</a>
        <a href="#commands">Commands</a>
        <a href="#status">Status</a>
        <a href="#privacy">Privacy</a>
      </nav>
    </div>
  </header>  <section id="home" class="hero">
    <div>
      <h1>All-In-One Discord Bot</h1>
      <p>Moderation, music
